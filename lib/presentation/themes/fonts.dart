import 'package:flutter/material.dart';

/*
  使用するフォント，文字の大きさをここにまとめる
*/

class BrandText {
  static const sizeM = TextStyle(
    fontSize: 24.0
  );
  static const sizeS = TextStyle(
    fontSize: 16.0
  );
}
